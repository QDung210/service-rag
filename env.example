# Environment
ENVIRONMENT=development

# Application Settings
PROJECT_NAME=RAG Service
VERSION=1.0.0
DESCRIPTION=Database Schema RAG Service with LightRAG, PGVector, and Neo4j
API_V1_STR=/api/v1
HOST=0.0.0.0
PORT=8000
DEBUG=true

# CORS Settings (JSON format)
ALLOWED_ORIGINS=["*"]

# PostgreSQL (PGVector)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DATABASE=postgres

# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_DATABASE=neo4j
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=neo4j_local_dev

# LiteLLM Proxy
LITELLM_URL=http://localhost:4000
# LITELLM_KEY là master key BẠN TỰ TẠO để authenticate với LiteLLM proxy
# Phải khớp với LITELLM_MASTER_KEY trong docker-compose.yml
# Development: Dùng sk-1234
# Production: Đổi thành key phức tạp hơn (ví dụ: sk-your-random-string-here)
LITELLM_KEY=sk-1234

# LLM Models (via LiteLLM)
LLM_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIM=1536

# OpenAI API Key (REQUIRED - LiteLLM sẽ dùng key này)
OPENAI_API_KEY=your_openai_api_key_here

# Directory Settings
WORKING_DIR=./rag_storage
SCHEMA_DOCS_DIR=./schema_docs
DATA_DIR=./data

# Logging
LOG_DIR=logs
LOG_LEVEL=WARNING
APPLICATION_LOG_LEVEL=INFO
LOG_FORMAT=console

# LightRAG Settings
MAX_ASYNC=4
MAX_TOKENS=32768
MAX_EMBED_TOKENS=8192

# Query Settings
TOP_K=10
